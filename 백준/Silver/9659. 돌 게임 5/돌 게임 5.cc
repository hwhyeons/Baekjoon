#include <bits/stdc++.h>
#include <ranges>

typedef long long ll;

using namespace std;

int main() {
    ll n;
    cin >> n;
    if (n%2 == 0) {
        cout << "CY";
    } else cout << "SK";
}