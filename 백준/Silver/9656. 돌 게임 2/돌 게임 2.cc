#include <bits/stdc++.h>
#include <ranges>

typedef long long ll;

using namespace std;

int main() {
    int n;
    cin >> n;
    if (n%2 == 1) {
        cout << "CY";
    } else cout << "SK";
}