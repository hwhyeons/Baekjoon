#include <bits/stdc++.h>
#include <ranges>

typedef long long ll;

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int tcnt;
    cin >> tcnt;
    while (tcnt--) {
        int N,M;
        cin >> N >> M;
        int left_cnt = N;
        int ans = 0;
        unordered_set<int> s;
        for (int i = 0; i < M; ++i) {
            int t1,t2;
            cin >> t1 >> t2;
        }
        cout << N-1 << "\n";
    }


}